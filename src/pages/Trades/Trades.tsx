import RockWithMessage from "@components/RockWithMessage";

import classes from "./styles.module.css";

const Trades = () => {
  return (
    <section className={classes.trades}>
      <RockWithMessage message="Still under development" />
    </section>
  );
};

export default Trades;
